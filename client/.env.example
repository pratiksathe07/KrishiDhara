# Client Environment Variables

# In development, Vite proxies /api → backend, so VITE_API_URL is not needed.
# In production, set this to your backend URL:
# VITE_API_URL=https://api.yourdomain.com/api
